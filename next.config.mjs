/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "preview.redd.it",
      "via.placeholder.com",
      "yt3.googleusercontent.com",
    ],
  },
};

export default nextConfig;
